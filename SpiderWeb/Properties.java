/**
 * Contains the properties of the Window and the animations.
 */
class Properties {

    static final int WINDOW_WIDTH = 800;
    static final int WINDOW_HEIGHT = WINDOW_WIDTH / 16 * 9;

    static final int POINT_SIZE = 5;
    static int POINTS = 30;
    static int CONNECTION_DISTANCE = 100;

    static boolean highQuality = true;
    static boolean drawPoints = true;
    static boolean drawConnections = true;
    static boolean drawPointCoordinates = false;
    static boolean drawPointIndex = false;

}